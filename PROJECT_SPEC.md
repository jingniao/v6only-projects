v6only 项目开发说明
1. 项目目标
开发一个中文 Linux 管理脚本 v6only，让用户自行维护域名清单，使清单中的 AI 域名通过 IPv6 连接，并提供完整的软件生命周期管理。

必须包含：

自动检查、安装必要依赖和后端软件。
中文菜单及命令行管理。
域名添加、删除、查看、导入、导出。
精确域名及通配子域支持。
sing-box TUN 主后端。
dnsmasq 备用后端及后端切换。
后台运行记录、操作审计及配置变更历史。
配置校验、应用、失败恢复和回滚。
软件升级、重装、停用、卸载及数据清理。
项目当前状态：规划阶段。不得把本文中的设计当作已实现或已通过测试的功能。

2. 给 CLI 开发助手的执行要求
2.1 第一阶段允许执行
在当前工作区创建 v6only/ 项目目录。
创建项目文档、源码、配置模板及测试文件。
执行不改变宿主机网络的静态检查和单元测试。
输出已实现功能、测试结果和待办事项。
如果目录已经存在，先阅读现有内容；不得直接覆盖用户文件。

2.2 未经用户另行授权，不得执行
在目标服务器安装依赖或后端软件。
修改系统 DNS、路由、策略规则或防火墙。
创建或启动接管网络的 systemd 服务。
开启 TUN 接管。
修改公网地址、RA、cloud-init、DynamicV6。
重启服务器。
执行实际卸载或数据清理。
开发授权不等于部署授权。

若环境存在安全策略拦截，应报告原因，不通过编码、改写命令或换工具绕过。

2.3 开发原则
先设计与测试，再部署。
使用选定稳定版本的官方文档验证配置语法。
不根据记忆直接生成未经验证的 sing-box 配置。
不把 DNS 过滤宣传为严格出站控制。
不承诺尚未实测的功能。
不为赶进度伪造测试通过结果。
涉及版本兼容性或技术限制时，在文档中明确记录。
3. 已确认需求



项目	要求
主后端	sing-box TUN
备用后端	dnsmasq
清单命中策略	IPv6-only；IPv6 不可用则失败，不回退 IPv4
通配	支持 *.openai.com
AI 范围	OpenAI/ChatGPT、Anthropic/Claude、xAI/Grok
管理语言	中文菜单、帮助、提示、注释
后台记录	持久化审计、服务日志、配置历史、健康报告
生命周期	安装、升级、重装、停用、卸载、清理
数据安全	卸载默认保留清单、记录及必要备份
网络安全	保留 SSH 和 DynamicV6 管理路径，支持自动回滚
建议默认值：

首版支持 Debian/Ubuntu + systemd；具体版本范围由测试确定。
仅接管本机默认网络命名空间内明确纳入的流量。
清单外流量 IPv6 优先，允许 IPv4 回退。
默认不对外开放 DNS 或代理端口。
不自动接管 Docker、其他命名空间、手机或电脑。
详细连接域名日志默认关闭。
4. 技术边界
4.1 sing-box TUN 主后端
数据路径：

text


本机程序
   ↓
TUN 接管
   ↓
域名识别与路由匹配
   ├─ 命中清单 → IPv6-only 出站
   └─ 未命中   → 默认双栈策略
   ↓
真实网络接口
域名识别可以结合：

DNS 映射。
代理请求中携带的域名。
支持的协议嗅探。
严格约束必须落实到出站解析和连接行为，不能仅依赖隐藏 A 记录。

保证范围限定为：

被接管、能够识别目标域名、命中规则的连接。

以下情况必须单独测试并说明限制：

直接连接 IP。
应用缓存的 IP。
独立 DoH。
ECH 或其他隐藏域名的情况。
接管前已建立的连接。
未接管的容器和其他网络命名空间。
默认将无法关联域名的连接作为普通流量处理，并明确说明这不是“任意应用、所有情况都无法绕过”的保证。

不得通过封禁共享 CDN 的全部 IPv4 来冒充精准域名控制。

4.2 dnsmasq 备用后端
定位：DNS 约束模式，保障程度低于主后端。

建议架构：

text


本地 DNS 前端
   ├─ 清单命中 → 独立过滤实例
   └─ 未命中   → 普通解析实例
注意：

filter-A 是全局过滤功能。
不使用未经支持的 filter-A=/域名/ 写法。
按域名分派与精确/通配语义必须验证。
需要检查 CNAME、HTTPS/SVCB 及地址提示记录。
无法阻止缓存 IPv4、独立 DNS 或直接 IP 连接。
切换到 dnsmasq 必须提示并确认：

即将切换为 DNS 约束模式，无法提供与 TUN IPv6-only 出站相同的保障。

如果无法正确表达清单规则，应拒绝切换并报告不兼容规则；不得静默扩大匹配。

5. 域名清单格式
5.1 语法
UTF-8 文本，一行一条，支持：

空行。
整行 # 注释。
空白后的行内注释。
精确域名。
通配子域。
示例：

text


openai.com
*.openai.com

api.anthropic.com  # 精确匹配
匹配语义：




规则	含义
openai.com	只匹配根域
*.openai.com	匹配一级、多级子域，不含根域
根域与通配规则分别保留，不能错误合并。

5.2 输入规范
统一转为小写。
规范化末尾点。
首版接受合法 ASCII DNS 域名及合法 punycode。
拒绝 URL、IP、端口、路径、控制字符。
星号仅允许出现在开头的 *.。
禁止通过 eval 或 shell 拼接执行域名输入。
必须测试误匹配：

text


abcopenai.com
openai.com.evil.example
后端后缀匹配可能包含根域，不能直接等同于“仅子域通配”。

5.3 初始示例清单
text


# OpenAI / ChatGPT
openai.com
*.openai.com
chatgpt.com
*.chatgpt.com
oaistatic.com
*.oaistatic.com
oaiusercontent.com
*.oaiusercontent.com

# Anthropic / Claude
anthropic.com
*.anthropic.com
claude.ai
*.claude.ai

# xAI / Grok
x.ai
*.x.ai
grok.com
*.grok.com
这是待验证示例，不保证每个域名都支持 IPv6，也不保证覆盖所有第三方资源。

无 AAAA 的域名在严格模式下应失败，不自动移除规则或改为 IPv4。

共享登录、共享 CDN 域名不得未经确认自动加入。

6. 管理脚本设计
6.1 交付形式
开发源码允许模块化，便于测试和维护。
发行目标为单文件 Bash 管理入口 v6only。
所需辅助工具由安装阶段检测和安装。
不宣称“零依赖”。
如需打包工具，应提供可复现的构建步骤。
无参数执行时展示中文菜单；非交互环境不应意外等待菜单输入。

6.2 命令接口
bash


v6only install --backend singbox
v6only install --backend dnsmasq

v6only add '*.openai.com'
v6only remove '*.openai.com'
v6only list
v6only import domains.txt
v6only export domains.txt

v6only apply --dry-run
v6only apply
v6only confirm

v6only backend use singbox
v6only backend use dnsmasq

v6only status
v6only check
v6only test api.openai.com
v6only doctor
v6only logs
v6only history

v6only update
v6only reinstall
v6only disable
v6only enable
v6only rollback

v6only uninstall
v6only purge
v6only purge --keep-logs

v6only help
v6only version
6.3 关键语义
add/remove/import：仅修改待应用清单。
apply：生成、校验、备份、临时应用、健康检查、确认提交。
confirm：仅确认当前事务并取消其专属回滚任务。
disable：用户主动解除接管及严格限制，明确提示影响。
backend use：事务化切换，失败恢复上一可用后端。
rollback：撤销本项目事务，检测用户后续修改冲突。
uninstall：恢复网络后移除运行组件，保留管理入口及数据。
purge：卸载验证成功后，经确认清除本项目数据。
所有写操作支持 --dry-run：

不安装软件。
不修改文件、网络或服务。
不写持久化审计。
明确列出计划变更。
需要联网查询的行为提前说明。
建议退出码：




码	含义
0	成功
1	运行错误
2	参数错误
3	前置条件不满足
4	配置或健康校验失败
5	恢复失败，需要人工处理
7. 安装与升级
7.1 安装流程
检查权限、系统、架构、服务管理器。
检查 TUN、DNS 管理方式、路由、防火墙及已有服务。
检测缺失依赖，展示安装计划。
记录原有包状态及本次新增包。
获取已测试的官方稳定版本。
校验摘要；有可用签名时验证签名。
创建项目专属配置与服务。
生成并校验候选配置。
在接管前准备独立回滚。
经明确授权接管并验证。
用户确认后提交。
要求：

不通过不透明的 curl | sh 安装。
不覆盖系统已有 sing-box/dnsmasq 实例。
通过包管理器安装可能自启的服务时，必须评估并控制自启副作用。
不支持的系统明确拒绝，不盲目适配。
重复安装应幂等。
7.2 升级流程
备份当前版本、配置和状态。
验证新版本配置兼容性。
先校验候选配置，再切换。
健康检查失败恢复旧版本。
不自动升级到测试版。
记录版本、下载来源、摘要和升级结果。
8. 文件布局
8.1 源码项目
text


v6only/
├── README.md
├── PROJECT_SPEC.md
├── docs/
│   ├── requirements.md
│   ├── development-checklist.md
│   ├── architecture.md
│   ├── cli-design.md
│   ├── domain-rules.md
│   ├── logging.md
│   ├── safety-and-rollback.md
│   ├── acceptance-tests.md
│   └── compatibility.md
├── src/
│   ├── v6only.sh
│   ├── lib/
│   └── backends/
├── templates/
├── examples/
│   └── domains.txt.example
├── tests/
│   ├── unit/
│   ├── integration/
│   └── fixtures/
└── tools/
尚未实现的模块必须明确标记，不要创建会假装成功的占位逻辑。

8.2 目标部署布局
text


/usr/local/sbin/v6only
/opt/v6only/
/etc/v6only/
/var/lib/v6only/
/var/log/v6only/
建议服务名称：

text


v6only-singbox.service
v6only-dns*.service
所有资源记录在项目归属清单中，包括：

文件路径及用途。
创建前是否存在。
文件摘要和必要元数据。
项目服务、定时器。
路由、策略规则、防火墙资源标识。
依赖安装记录。
9. 事务、安全与回滚
9.1 事务流程
text


环境检查
  ↓
保存当前状态
  ↓
生成候选配置
  ↓
静态校验
  ↓
安排独立定时回滚
  ↓
临时接管
  ↓
健康检查
  ↓
用户确认
  ↓
提交
定时回滚必须：

独立于 sing-box 服务运行。
SSH 断开后仍然有效。
具有唯一事务标识。
能在重复执行时安全处理。
不取消其他项目或其他事务的任务。
9.2 网络保护
保留 SSH 返回路径。
保护 DynamicV6 管理连接。
防止后端自身出站进入 TUN 循环。
检查实际路由优先级与选路，不仅检查表号是否不同。
不整体清空路由、防火墙或策略规则。
不把当前动态 IPv6 地址写死为永久配置。
9.3 故障保护
自动重启服务不是严格防泄漏保证。

必须设计并测试：

sing-box 异常退出。
DNS 服务失败。
IPv6 路由消失。
候选配置应用失败。
回滚时服务无法启动。
若故障状态下无法区分域名，保护措施可能影响更广泛的非管理 IPv4 流量。必须提前说明并确认影响范围，不暗中恢复直连。

无法实现可靠故障保护时，应报告限制，不能宣称全故障场景严格保障。

系统 DNS 不得配置绕过过滤的公共备用解析；多个上游也必须经过相同策略。

10. 日志与后台记录



类型	内容
audit	时间、操作者 UID、脱敏命令、事务编号、结果
changes	清单/配置差异、版本、应用及恢复记录
health	DNS、选路、TCP/TLS、服务状态及错误类别
service	后端原始日志和中文诊断摘要
要求：

重启后记录仍然保留。
默认仅 root 可访问敏感记录。
不记录 API Key、Cookie、Authorization、聊天正文或带凭据 URL。
详细域名连接记录默认关闭。
日常日志建议保留 30 天。
健康记录建议保留 90 天。
日志总容量建议限制为 200 MiB。
备份数量另设上限，不删除唯一恢复快照。
轮转后验证服务正确重新打开日志文件。
可选周期体检使用项目专属 systemd timer，默认不启用。
未启用周期体检时，不宣称已经持续检测网络健康。
11. 卸载与清理
11.1 uninstall
执行顺序：

检查当前状态和外部修改冲突。
准备恢复路径。
退出接管并恢复本项目修改的网络和 DNS。
验证恢复结果。
移除专属服务及后端运行组件。
保留清单、记录、必要备份和管理入口。
输出保留与移除清单。
恢复失败则停止清理，保留恢复所需文件。

11.2 purge
在卸载验证成功后，经明确确认清理：

项目配置和域名清单。
项目日志，除非指定 --keep-logs。
项目历史、状态及备份。
项目专属定时任务和其他归属资源。
可选删除管理入口。
依赖包默认保留；可选清理时检查共享使用，不仅根据“最初由本项目安装”就删除。

禁止：

恢复整份过时网卡配置覆盖用户后续修改。
删除 DynamicV6。
修改现有 RA、cloud-init 设置。
删除其他项目配置或网络备份。
清除全系统 journal 或包管理日志来制造“无痕卸载”。
12. 目标服务器历史参考
以下仅用于提醒兼容风险，部署前必须重新勘察：

text


IPv4：198.51.100.23
IPv6：2001:db8:1::10
网卡：eth0
IPv6 管理组件：DynamicV6
历史策略规则优先级：25423
历史路由表：47423
历史操作包括：

移除了原 2602 IPv6 地址。
关闭了 eth0 的 RA 接受。
配置过 cloud-init 网络配置禁用。
DynamicV6 保持启用。
这些都是现有环境，不属于 v6only 应撤销的改动。

不要使用 /root/net-snapshots/ 中旧网络实验备份作为本项目回滚基线；本项目必须在安装当时创建自己的快照。

不要根据历史地址、租约时间或服务状态假设当前仍然有效。

13. 开发清单
阶段一：设计与管理骨架
 确定系统和版本支持范围。
 核实 sing-box 稳定版本与字段兼容性。
 定义后端适配接口。
 中文菜单、帮助、参数校验、退出码。
 并发锁和权限管理。
 无副作用 --dry-run。
 事务状态及归属清单。
 完成基础文档。
阶段二：清单和配置
 域名规范化与输入验证。
 精确/通配匹配测试。
 add/remove/list/import/export。
 编辑与应用分离。
 候选配置生成与校验。
 配置历史及失败恢复。
阶段三：主后端
 依赖与 sing-box 安装器。
 独立服务和配置。
 TUN 接管与管理流量绕过。
 域名识别及 IPv6-only 出站。
 默认双栈策略。
 定时回滚与确认。
 动态 IPv6 变化处理。
 故障保护及崩溃测试。
 DynamicV6 多轮共存验证。
阶段四：备用后端
 dnsmasq 版本语法验证。
 独立实例与域名分派。
 精确/通配兼容性验证。
 CNAME、HTTPS/SVCB 测试。
 降级确认。
 后端切换与失败恢复。
阶段五：运维功能
 status/check/test/doctor。
 audit/changes/health/service 记录。
 日志轮转、容量与脱敏。
 update/reinstall。
 disable/enable/rollback。
 uninstall/purge。
 依赖共享检查。
阶段六：验收交付
 静态检查与单元测试。
 隔离环境集成测试。
 安装、升级、切换和卸载测试。
 异常与恢复测试。
 经用户授权进行目标机器验证。
 经用户授权进行重启验证。
 完整使用手册与排障说明。
 未实现功能及已知限制清单。
14. 验收标准



测试	预期结果
根域和通配	严格遵守“不含根域”的通配语义
相似域名	不误匹配
非法输入	拒绝，不执行输入内容
无效候选配置	不破坏当前可用服务
命中、有 AAAA	实际连接使用 IPv6
命中、只有 A	连接失败，无对应 IPv4 出站
命中、IPv6 故障	不回退 IPv4
未命中	按默认双栈策略工作
后端异常退出	符合预先说明的故障保护行为
SSH 与管理连接	接管前后可用
DynamicV6 多轮对账	无路由循环或策略冲突
地址变化	不继续使用失效租约地址
后端切换失败	恢复上一可用状态
升级失败	恢复旧版本和配置
定时回滚	SSH 断开后仍可恢复
uninstall	网络恢复、组件移除、记录保留
purge	只删除项目归属资源
日志	持久化、轮转正常、无敏感凭据
重启	经授权实测服务、规则和网络状态
每项测试记录：

text


测试编号：
软件版本：
执行时间：
环境：
执行命令：
预期：
实际输出：
结论：
限制或备注：
DNS、TCP/TLS 连通性和业务访问权限分开判定：

HTTP 401/403 不直接等于网络故障。
IPv6 连通不保证 AI 服务地区或账号可用。
单一站点 ping 延迟不代表所有网站性能。
不使用真实密钥作为测试夹具。
未执行的测试标记“未执行”，不能标记“通过”。
15. 第一轮交付要求
CLI 第一轮先完成：

项目目录和中文文档。
管理脚本骨架。
域名解析、增删查、导入导出。
单元测试。
不修改系统的配置预览能力。
后续主后端开发计划。
网络接管、安装器和其他未完成命令，应明确返回：

此功能尚未实现，未修改系统。

不要为了展示完整菜单而让空实现返回成功。

每一轮完成后，向用户报告：

创建和修改的文件。
已实现的命令。
实际执行的测试及结果。
尚未实现的功能。
是否产生宿主机修改。
自动进入下一阶段计划。
未获得新的明确授权，不在目标服务器执行安装或接管。