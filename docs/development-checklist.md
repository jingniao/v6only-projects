# 开发完成情况与待验收项

## 已交付代码

- [x] 中文菜单、帮助、参数检查、退出码、非交互行为。
- [x] 域名规范化、精确/通配、增删查与导入导出。
- [x] 待应用与运行状态分离、并发锁、原子文件提交。
- [x] 所有写命令 dry-run，包括完整运行配置和网络计划预览。
- [x] 官方固定版本文档/源码核验、摘要下载、候选校验。
- [x] sing-box TUN/FakeIP、UID/接口范围、IPv6-only 与默认双栈。
- [x] dnsmasq 三实例、精确/通配、地址提示过滤、降级确认。
- [x] 唯一事务、独立恢复 unit/timer、确认、冲突和失败恢复。
- [x] 安装、依赖自启控制、升级、重装、停用和启用。
- [x] 卸载先验证恢复，清理只触及归属数据，默认保留入口/依赖。
- [x] 审计、变更、健康、私有日志、容量与快照保留。
- [x] 完整中文使用、架构、兼容性、运维与排障文档。
- [x] 可复现单文件 Bash 发行入口与静态载荷摘要检查。

## 本地和隔离验证

- [x] 单元测试覆盖并发、输入、dry-run、事务、依赖失败、恢复冲突、日志和清理。
- [x] 官方 sing-box check、dnsmasq --test。
- [x] nftables 语法与独立命名空间内真实规则验证。
- [x] systemd unit/timer 静态验证（未启动服务）。
- [x] 回环 DNS 精确/通配、CNAME、AAAA、HTTPS/SVCB 和过滤实例失败。
- [x] 独立命名空间 TCP/TLS/UDP、无 AAAA、IPv6 路由丢失、DNS 故障、后端崩溃。
- [x] 策略规则多轮更新、动态 IPv6、缓存映射、不同命名空间范围测试。

以本机最新 `python3 -B tools/check.py` 输出为准；本清单不替代逐项输出。第 16 轮本地全套（B01/U01/U02/S01/S03/D01/C01/I01/I02，单测 89 项）、第 17 轮（真机：BUG-15-01 复验、超时自动恢复、手动 rollback、SIGKILL 自愈、真实 SERVFAIL、客户端 DoH）与第 18 轮（真机：APT 安装窗口、整机冷启动）的同组检查均已通过。逐轮记录不随仓库分发。

## 目标服务器验收进展

- [x] Debian 13 上的安装、升级/重装、停用/启用、卸载与清单变更（第 12–15 轮，真机 live）。
- [ ] Debian/Ubuntu **各**目标发行版完整流程：仅 Debian 13 已验，Ubuntu 及其他发行版无对应机器。
- [x] 真实 SSH 会话及实际 DynamicV6 服务多轮对账（第 14、15、17 轮；`ip rule 25423` / `table 47423` 全程只读未受影响）。
- [x] 实际 systemd 定时恢复在真机真实运行（第 15 轮 A3-1、第 17 轮 §2：超时触发、专属单元清理、恢复后 `check` 通过）。
- [x] 真实 SERVFAIL 下不回退 IPv4（第 17 轮 §5：真实上游 SERVFAIL、健康检查失败后自动恢复、并行探针无 IPv4 回退）。
- [x] SIGKILL 级后端死亡、fail-closed 与自动恢复、守护单元 `_guard` 幂等（第 17 轮 §4）。
- [x] 手动 `rollback` 路径真机验证（第 17 轮 §3）。
- [x] journal namespace 持久化、可读性与命名空间容量观测（第 17 轮 §7）；轮转与容量上限沿用全局默认策略，未单独验证。
- [x] 客户端 DoH 路径（第 17 轮 §6）。ECH 因本机 libcurl 不支持 `--ech` 无法验证；既有长连接与更广泛缓存场景未验。
- [x] 实际 APT 安装窗口、自启策略和共享包状态恢复：第 18 轮 §1（真机 live，用户授权）——真机依赖已齐备、工具不会自发进窗口，故先 `apt-get remove -y dnsmasq-base`（叶子包）再 `v6only backend use dnsmasq` 触发重装；窗口 3.29 s（`policy-rc.d` 08:42:33.319Z 出现 mode 755 → 08:42:36.608Z 恢复为不存在），窗口内 `invoke-rc.d --query` 被拒（exit 101），全部 `apt`/`dpkg` 运行采样与日志时间戳均落在窗口内、窗口外零次；`dbus`/`ssh` 单元 `NRestarts=0` 且 ActiveEnter 仍为开机时刻（未自启、未重启）；`dependency_records` before `config-files` → after `installed`、`retained: true`；事后 `check` exit 0、快照 29/33/2。
- [x] 重启后服务、策略、缓存与网络状态：第 18 轮 §2（用户授权，重启由用户本人执行）——`boot_id` 更换、项目专属 journal 命名空间出现第 2 个 boot；guard（oneshot）与 sing-box 均 enabled 自动拉起（开机 18 s 内，`NRestarts=0`）；nft/`ip rule`/`table 47620` 路由/TUN/监听端口与重启前逐行一致（内核态由 guard 冷启动重建）；FakeIP 映射由落盘缓存恢复（openai `fc00::3`、google `fc00::2`、jd `fc00::6`，缓存库二进制核对）；uid1500 业务与 `check`（exit 0）恢复；保护单元 `dynamicv6-next-guest.service` 未受影响。观察项 OBS-18-01：冷启动存在约 14 s「fail-closed 规则尚未安装」窗口（系统内无更早的持久化规则加载路径；本机 boot 时无 uid1500 常驻进程，窗口内无流量来源）。
- [ ] 极端断电和内核动作/日志持久化窗口的恢复验证：无法构造。

这些是部署验收边界，不是已经通过的功能。第 18 轮两项破坏性验收（APT 安装窗口、整机冷启动）均由用户明确授权后执行，其中重启由用户本人操作（代理侧重启命令被安全策略拦截，未使用任何替代路径）；当前仍为实验版，后续目标验收须另行明确授权；不会为了补齐勾选在宿主机直接开启接管或重启。
